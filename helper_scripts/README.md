# Run `npm run build` each time changes are made to the ts file

# launch via debug config OR command line from the project root `node /helper_scripts/lib/index -s './src/entity/**/*.ts' -o 'src/util/EntityDescriptor.ts' `

## -s : source folder path

## -o : destination file path (.ts file)
